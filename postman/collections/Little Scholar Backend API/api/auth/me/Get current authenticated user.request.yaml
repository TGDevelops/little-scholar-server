$kind: http-request
url: "{{baseUrl}}/api/auth/me"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Get current authenticated user.resources/examples
order: 1000
